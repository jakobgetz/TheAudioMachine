export default interface Theme {
    primary: string
    primaryLight: string
    accent: string
    nav: string
    playHead: string

    //Sequencer Notes
    layerColorsActive: string[]
    layerColorsInactive: string[]

    //Beatsound Colors
    beatColors: string[]
}